# Default values for python.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
forceNodeType: gpu # matches the spotinst elastigroup node label
image:
  repository: draft
  tag: dev
  pullPolicy: IfNotPresent
service:
  name: REPLACE_ME_APP_NAME
  internalPort: 8000
resources:
  # corresponds to allocating most of p3.2xlarge capacity
  # https://ec2instances.info/?filter=p3.2xlarge
  limits:
    cpu: 8
    memory: 58Gi
    nvidia.com/gpu: 1
  requests:
    cpu: 8
    memory: 58Gi
    nvidia.com/gpu: 1
env:
  production:
    - name: foo
      value: bar
  development:
    - name: foo
      value: bar
